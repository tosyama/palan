ccall printf(...);

const N,M = 3, 9+2;
const NN = N+3;
printf(TEST_STR, M);

[NN]byte a;
f(a);

func f([N*2]byte n)
{
	[5]byte arr;
	printf(TEST_STR, N);
	arr -> f();

	func f([N]byte n) {
		printf(TEST_STR, N);
		const TEST_STR = "test3 %d\n";
	}
	const N = 5;
	const TEST_STR = "test2 %d\n";
}

printf(TEST_STR, N);

const TEST_STR = "test1 %d\n";

