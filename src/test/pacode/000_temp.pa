
type obj {
	// int64 x, y; //NG
	int64 x;
	int64 y;
};

// move work? => OK!
obj o = creObj();
{
	calcObj(creObj());	// NG! only top? in block?
}

[10][2]int32 arrarr;
afunc(arrarr);
// aafunc(arrarr);	// NG!

func creObj() -> obj { // no local? => OK!
	obj o;
	return o;
}

func calcObj(@obj o) {
}

func afunc(@[?][2]int32 arrarr)
{
}

func aafunc(@[?][?]int32 arrarr)
{
}

// NG! Abort
// func retlit() -> [3]int32
// {
//	return [1,2,3];
// }


// NG! Abort
// func retobj() -> obj o
// {
//	return [1,2];
// }

