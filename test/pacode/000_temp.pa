ccall int32 printf();
ccall exit();
ccall setenv();
ccall mtrace();
ccall muntrace();


setenv("MALLOC_TRACE", "out/mtrace000", 1);

mtrace();
{
	// alloc only
	int32[10] a;
	arr_func2(a>>);
}
muntrace();

printf("%d", 333);

exit(0);

func arr_func2(int32[10] >> pa)
{
}
