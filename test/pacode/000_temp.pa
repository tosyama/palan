ccall int32 printf();
ccall exit();

const N = 4-1;
const M = "%d\n";

int32 a, b;
int64[N] i, j;

N->i[N-3];
i -> pa(N) -> j;
printf(M, j[1]);

exit(0);

func pa(int64[N] a, int32 b) -> int64[N] a
{
	func priv(int32 a, int32 b)->int32 c
	{ a+b -> c; }

	const A = 1;
	{
		A-1, b -> priv() -> a[A];
	}
}

func p(int32 a, b) -> int32 c, d
{
	printf("%d %d\n", a, b);
	a+b -> c;
	a-b -> d;
	a,b -> priv() -> c;
}

func p() -> int32
{
	return 1;
}
