ccall printf(...);

const N,M = 3, 9+2;
const NN = N+3;
printf(TEST_STR, M);

byte[NN] a;
f(a);

func f(byte[N*2] n)
{
	byte[5] arr;
	printf(TEST_STR, N);
	arr -> f();

	func f(byte[N] n) {
		printf(TEST_STR, N);
		const TEST_STR = "test3 %d\n";
	}
	const N = 5;
	const TEST_STR = "test2 %d\n";
}

printf(TEST_STR, N);

const TEST_STR = "test1 %d\n";

