ccall int32 printf();

int64 a = 1;
int64 b,c,d = 2,1+2,4;
int64 e,f = -1,-2;
int64 h;
int64 i,j = 0, 1-4;
{
	int64 k = 3;
	-k -> e;
}

a+d -> h;
e+1-f+-d+(-1) -> f;

int64 k,l = add(5,6);
int64 m = 1+(3+2*b)*c*4*5;

printf("%d %d %d %d %d %d %d\n", h, j, f, a+b+c, k, l, m*3);

a+b+c; // only calc
-a;
a*c;
a/c;
a%c;

1+2*c, 5/2*3%2, d/(-2)*5, a*b*(c-2)/(c-1)
	-> a,b,c,d;

printf("%d %d %d %d\n", a, b, c, d);


func add(int64 a, b)
	-> int64 aa, ab
{
	int64 c = 2;
	a+c-1,b+2 -> aa,ab;
}
